# ============================================================
# DataPipe-RSS — Python Dependencies
# Install with: pip install -r requirements.txt
# ============================================================

# --- Core RSS Parsing ---
feedparser==6.0.11          # The primary RSS/Atom feed parser
requests==2.31.0            # HTTP client for webhook calls
urllib3==2.1.0              # HTTP library (requests dependency)

# --- Environment & Config ---
python-dotenv==1.0.0        # Loads .env into os.environ

# --- Database ---
# sqlite3 is part of Python stdlib — no install needed

# --- Scheduling (for standalone runner) ---
schedule==1.2.1             # Lightweight in-process job scheduler

# --- Data Processing ---
python-dateutil==2.8.2      # Robust date/time parsing from feed entries
beautifulsoup4==4.12.2      # HTML stripping from feed descriptions
lxml==5.1.0                 # Fast XML/HTML parser (bs4 backend)

# --- Logging ---
colorlog==6.8.2             # Colored console log output

# --- Future: AI Processor (uncomment when needed) ---
# openai==1.12.0

# --- Future: Telegram Connector (uncomment when needed) ---
# python-telegram-bot==20.7

# --- Future: Notion Connector (uncomment when needed) ---
# notion-client==2.2.1

# --- Dev / Testing ---
pytest==8.0.2
pytest-mock==3.12.0
